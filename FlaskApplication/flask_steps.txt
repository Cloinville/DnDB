1. pip3 install flask
2. pip3 install flask-mysqldb
**NOTE: flask python application CANNOT be named flask.py, else conflicts**
3. create 'templates' folder in same folder as flask_app.py, for use by render_templates: will hold html files for each page of the application